# 115 서버(2×RTX3080) 실행용 — 노트북 requirements.txt + GPU/딥러닝 추가
# 설치: pip install -r requirements.txt -r requirements-server.txt
#   (torch는 CUDA 버전에 맞춰: https://pytorch.org 의 cu12x 인덱스 사용)

# 기존(노트북 공통)
-r requirements.txt

# ── 딥러닝(GPU) ──
# torch/torchvision : CUDA 빌드로 설치(아래는 예시, 서버 CUDA에 맞게)
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1
torchvision>=0.16

# ── V2V (Vision-to-Vector, P2 v2) — 인스턴스 세그멘테이션 ──
ultralytics>=8.1          # YOLOv8-seg (설치·사용 용이, 2×3080 적합)
# 또는 detectron2(Mask R-CNN 정석): 별도 설치(소스 빌드)
pycocotools>=2.0          # COCO 평가/마스크

# ── 생성 모델 (P3) — 그래프 신경망 ──
torch-geometric>=2.4      # GNN/graph-diffusion
# torch-scatter, torch-sparse 는 torch/CUDA 버전 맞춰 별도 설치 권장

# ── 실험·시각 (선택) ──
# wandb                   # 실험 추적(선택)

# ── 위상 편집기(✏️) — 캔버스 그리기 ──
streamlit-drawable-canvas   # 원본 위 박스/폴리곤/선 그리기(사람 위상 구축)
streamlit-option-menu       # 사이드바 클릭형 메뉴(동그라미 없는 네비)
