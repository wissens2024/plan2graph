# 중복 도면 정리 — 현황 (DEDUP_STATUS)

> 목표: **같은 도면을 여러 번 보정하는 중복**을 줄여 알바 작업량 감축.
> 원칙: 원본 불변 · 되돌리기 가능 · 사람 보정 보호 · 삭제 없음.
> 이 문서 = 우리 둘이 보는 단일 현황판. (숫자는 2026-08-06 기준, 재현 가능)

숫자를 다시 뽑으려면:  `python scripts/dedup_index.py`  (약 1분)
눈으로 보려면:        에디터 사이드바 **🔁 중복 검수** → `/editor/dedup`
표로 보려면:          [DEDUP_CONFLICTS.md](DEDUP_CONFLICTS.md) (자동 생성)

**작업 초점 = APT · 1세대 평면도.** 화면 기본 필터도 그렇게 맞춰 놨다.

---

## 1. 현황 숫자

| 항목 | 값 |
|---|--:|
| 전체 도면(세대 단위) | 41,409 |
| 보정 완료 | 5,248 |
| **고유 도면(동일도면 = 위치·미러 무관)** | **17,847** |
| 중복률 | 57% |

## 2. 평면 구분 — 1세대 vs 층 (⚠ 현관 수만으로 자르면 안 된다)

전실이 **전부 '현관'으로 오라벨**돼 있어([[vestibule-vs-entrance-mislabel]]) parsed 현관 수는 부풀려져 있다.
보정본 대조 실측:

| parsed 현관 | 사람이 고친 결과 | 건수 | 뜻 |
|---|---|--:|---|
| 1 | 현관1 · 공용부 없음 | 2,732 | 1세대 ✅ (정확도 95%) |
| 1 | 공용부 있음 | 101 | 실은 층 평면도 |
| **2** | **현관1로 정정 · 공용부 없음** | **1,326** | **실은 1세대** ← 전실 오라벨 |
| 2 | 현관2 · 공용부 있음 | 598 | 층 평면도 ✅ |

→ 채택한 규칙 (`classify_scope`):
- 보정본 있음 → 사람이 고친 결과(공용부 유무 · 현관 수)로 **확정**
- 보정본 없고 현관 ≤1 → `1세대` (정확도 95%)
- 보정본 없고 현관 ≥2 → **`판정보류`** (절반은 1세대이므로 '층'으로 단정 금지)

공용부 = 엘리베이터 · 엘리베이터홀 · 계단실. parsed 파서는 이 역할을 못 내고(41,409장 중 8장),
사람이 보정하며 넣는다. 그래서 미보정 도면은 공용부로 판정할 수 없다.

### 구분별 규모

| 구분 | 그룹 | 도면 | 보정 | 미보정 | 불일치 그룹 |
|---|--:|--:|--:|--:|--:|
| **APT · 1세대 평면도** | **15,249** | **34,943** | **3,722** | **31,221** | **376** |
| APT · 층 평면도 | 393 | 3,423 | 1,430 | 1,993 | 140 |
| APT · 판정보류 | 567 | 783 | 0 | 783 | 0 |
| ROW · 1세대 | 729 | 1,151 | 7 | 1,144 | 0 |
| DEH · 1세대 | 534 | 576 | 68 | 508 | 2 |
| (그 외) | 375 | 533 | 21 | 512 | 2 |

> ⚠ **보정 5,248건 중 1,430건(27%)이 층 평면도에 쓰였다.** 1세대가 목표라면 그만큼이 헛일이었다.
> 층 평면도는 도면 3,423장뿐(8%)인데 보정은 27%를 가져갔다 — 사본이 많고 방이 많아 눈에 잘 띄어서다.

## 3. APT · 1세대 기준 작업량

| 구분 | 그룹 | 건수 | 처리 |
|---|--:|--:|---|
| **① 안전 전파**(불일치 없는 그룹의 미보정 형제) | 1,974 | **3,470** | 지금 채울 수 있음 |
| ② 불일치 그룹 | **376** | 보정 1,723 / 미보정 9,169 | **사람이 눈으로 골라야 함** |
| ③ 아직 아무도 안 건드림 | 12,899 | — | 큐 dedup 후 진행 |

## 4. 불일치가 왜 생기나 (표본 관찰)

알바마다 판단이 갈리는 지점이 반복된다:

- **파우더룸 유/무** — 같은 칸을 욕실 일부로 볼지 파우더룸으로 뗄지
- **알파룸 vs 기타 vs 다목적공간** — 애매한 여유 공간의 이름
- **펜트리 개수** — 주방 수납을 1개로 볼지 2개로 쪼갤지
- **대피공간 / 실외기실** 인식 여부

> ※ 최악 사례: 한 도면(사본 320장)을 **146번**, **90가지로 다르게** 보정 — 이건 층 평면도다.
> 1세대 최악은 사본 111장을 48번·24가지(`615172ccb76a7784`).

## 5. 검증 결과

| 항목 | 결과 |
|---|---|
| 서명 방식 | 방 폴리곤 좌표 → 평행이동·미러 정규화 → 1px 양자화 → 해시 |
| 전파 방식 | 좌표 변환 + **대상별 정렬검증**(2px 초과면 자동 스킵) |
| 표본 정렬오차 | **0.0px** (12개 그룹 표본, 형제 위 정확히 얹힘) |
| 전파 왕복시험 | 생성본 bbox·중심좌표가 대상 원본과 완전 일치 · 역할은 원본 보정과 동일 ✅ |
| 서명 신뢰도 | 사본 2+ 그룹 5,253개 중 좌표 20개 미만(빈약) = **46개(0.9%)** → 화면에 `서명약함` 경고 |
| 평면 구분 | 현관≤1 → 1세대 정확도 **95%** (보정본 2,833건 대조) |

## 5-1. 중복이 진짜인가 — 원본 픽셀 전수 검증 (2026-08-07)

도형 서명으로 묶고 도형 서명으로 검증하면 자기순환이다. 그래서 **원본 PNG 픽셀**을 직접
비교했다 — 파싱된 폴리곤과 무관한 증거다. `scripts/dedup_verify_pixels.py` (읽기 전용).

**표본이 아니라 전수**: 사본 2장 이상인 **5,253개 그룹 · 도면 28,815장 · 시트 10,775개 전부**.
시트당 1회만 디코딩하고, 미러 그룹은 정규 방향으로 뒤집어 맞춘 뒤 비교. 판정불가 0건.

| 판정 | 그룹 | 뜻 |
|---|--:|---|
| ① 픽셀 완전 동일 | 4,342 | 잘라낸 이미지가 바이트 단위로 같음 |
| ② 렌더 잡음만 | 610 | 이진화하면 동일(안티에일리어싱 수준) |
| ③ 주석/라벨만 차이 | 212 | 벽·기구 같고 텍스트·가구 레이어만 다름 |
| ④ 그 외 — 사람이 볼 것 | **89** | 픽셀이 유의하게 다름 |
| **→ 같은 도면** | **98.31%** | |

- 방법 검증(음성 대조군): 서로 다른 60개 그룹의 대표를 비교해 정확히 60종이 나왔다.
- **사본은 전부 다른 시트에서 왔다** — 320장짜리 그룹 = 서로 다른 320개 시트.
  시트 안의 세대 중복이 아니라, **데이터셋 전체에 같은 평면형이 반복 등록**된 것.
- 서명이 얼마나 엄격한가: 사본 2장 이상 그룹의 **90.1%가 좌표 100개 이상**이 1px 이내로
  전부 일치해서 묶였다(320장 그룹 = 좌표 272개 일치). 20개 미만은 46개(0.9%)뿐.

### ⚠ 진짜 문제는 따로 있었다 — 주석이 딴 도면에 얹힌 원본 오류 144장

④를 파고드니 그룹이 잘못 묶인 게 아니었다. 28장짜리 그룹에서 **27장은 픽셀 차이 0.0%,
1장만 131% 달랐다.** 그 1장에 파싱된 폴리곤을 겹쳐 그려보니 **그림과 전혀 맞지 않았다**
(정상 도면은 벽에 정확히 얹힌다). 즉 그 AI-Hub 기록은 **다른 도면의 주석을 갖고 있다.**

- 이런 도면 **144장**을 전수에서 찾아냈다. 그중 **14장은 이미 보정까지 끝나 있었다**
  (알바가 그림과 무관한 주석을 보고 작업 — 그 노동은 헛간 것).
- 37개 그룹에 걸쳐 있다.
- 즉 우리 그루핑은 **파싱된 주석에 충실했고**, 픽셀 검증이 잡아낸 건 **원본 데이터 오류**다.
- 이 검증은 dedup을 넘어 **데이터 품질 도구**로 쓸 수 있다(학습 데이터에서 빼야 할 도면).

### 반영

- 전파는 이 144장을 **원본으로도, 대상으로도 쓰지 않는다**(자동 차단·스킵).
- 🔁 중복 검수 화면에 `원본 픽셀: …` 배지와 `주석-그림 어긋남 N장` 경고를 띄운다.
- 산출: `data/staging/dedup_pixel_verify.json` (그룹 판정 + 어긋난 도면 목록)

### 남은 한계

- ④ 89개 그룹은 아직 사람이 안 봤다(화면에서 `픽셀불일치` 배지로 표시).
- 사본 1장뿐인 그룹(12,594개)은 비교 대상이 없어 검증 자체가 불가능하다.
- 서명이 빈약한 46개 그룹(좌표<20)은 계속 `서명약함` 경고로 남긴다.

## 6. 도구

| | 무엇 |
|---|---|
| `scripts/dedup_index.py` | 전체 스캔 → 그룹·변종·불일치·평면구분 계산. **읽기 전용**. 산출: `data/staging/dedup_index.json`, `docs/DEDUP_CONFLICTS.md` |
| `src/plan2graph/dedup.py` | 서명·보정비교·좌표변환 핵심 |
| `src/plan2graph/dedup_review.py` | 🔁 중복 검수 화면 + API(`/dedup`) |
| 화면 필터 | **주거(APT 기본) · 평면(1세대 기본) · 상태 · 정렬** |
| 화면 기능 | 그룹 목록 → 변종 카드 나란히(원본 도면 위에 역할 색칠) → **다수안 대비 차이**(`+파우더룸` `−펜트리`) → 골라서 전파 |
| 전파 버튼 2종 | `미보정 N건 채우기`(안전, 기존 보정 안 건드림) · `그룹 전체 통일`(덮어씀 — **자동 백업** 후) |
| 백업 위치 | `data/staging/corrected/_dedup_backup/<시각>_<그룹>/` |
| 출처 기록 | 전파본 meta에 `correction_source=propagated`, `propagated_from`, `dedup_group` |

## 7. 진행 상태

| | 상태 |
|---|---|
| 중복 그룹·불일치 계산 (재현 가능) | ✅ |
| 1세대 / 층 평면도 구분 + 화면 필터 | ✅ |
| 불일치 표(도면번호 포함) | ✅ `docs/DEDUP_CONFLICTS.md` |
| 🔁 중복 검수 화면(비교·전파) | ✅ 배포 |
| 좌표변환 전파 + 정렬검증 | ✅ 검증 완료 |
| **APT·1세대 안전 3,470건 전파** | ⏸ 대기 |
| **에디터 큐 dedup + 1세대 필터** | ✅ 배포 — 큐 34,917장 → **고유 15,249건**으로 접힘 |
| 판정보류 1,455장(현관2+·미보정) 확정 | ⏸ 미착수 |

## 8. 보정 큐 사용법 (알바에게 그대로)

에디터 사이드바 기본값 = **APT · 단위세대 · 🔁 같은 도면 묶기 켜짐**. 그대로 두면 된다.

1. 목록 한 줄 = **고유 도면 하나**. 뒤의 ×111 은 같은 도면이 111장이라는 뜻.
   대표는 **아직 보정 안 한 사본**이 자동으로 뽑힌다(끝난 걸 또 열지 않게).
2. 평소처럼 보정하고 **저장**.
3. 저장 후 사이드바의 "🔁 같은 도면 N장 · 미보정 N건" 박스에서
   **[이 보정을 형제 N건에 복사]** → 한 번에 처리된다.
   · 이미 보정된 형제는 **건드리지 않는다**(fill 모드).
   · 형제끼리 보정이 갈린 그룹은 🔁 중복 검수 화면에서 골라 통일한다.

### 전파 버튼 2종 차이 — "채우기 = 빈칸만, 통일 = 빈칸 + 다른 답 고치기"

| | 미보정 N건 채우기 | 그룹 전체 통일 |
|---|---|---|
| 대상 | 아직 아무도 보정 안 한 형제만 | 미보정 + **다르게 보정된** 형제 |
| 기존 사람 보정 | 절대 안 건드림 | 덮어씀(자동 백업) |
| 이미 같은 보정인 형제 | 대상 아님 | 건너뜀 |
| 되돌리기 | 생성된 파일만 삭제 | `_dedup_backup/<시각>_<그룹>/` 에서 복원 |
| 성격 | 안전 — 새 데이터만 채움 | 파괴적 — 불일치를 하나로 정리 |

## 9. 다음 액션 (추천 순)

1. **APT·1세대 안전 3,470건 전파** — 즉시 "보정 완료" +3,470.
   큐에서 한 건씩 눌러도 되고, 일괄 실행을 원하면 별도 결정.
2. **APT·1세대 불일치 376그룹 검수** — 🔁 화면 기본 필터가 이미 여기에 맞춰져 있다.
3. **판정보류 1,282장 확정** — 현관 2+ 이면서 미보정이라 1세대/층을 못 가른 도면.
4. 옛 표본 전파 12건(2026-08-05)의 메타 스키마 정규화 —
   `correction_source` 가 옛것은 '출처 plan_id', 새것은 문자열 'propagated' 로 두 의미.
   판별은 양쪽 공통인 `meta.propagated` 로 할 것.
