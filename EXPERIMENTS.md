# 생성모델 실험 결과 — 글로벌 사전학습 효과 (v2)

> 3단계(공간배치 생성 AI)의 데이터·온톨로지 위에서 진행하는 실험. 본 문서는 **현재까지 완료된 학습**의 결과 보고이며, 향후 재학습 결과로 **업데이트**한다.
> 집계: `python -m plan2graph.experiments agg` (원장 `runs/index.jsonl`). 최종 실행 2026-06-03, ALL DONE 07:10.

---

## 1. 실험 목적
글로벌 사전학습셋(CubiCasa)으로 **사전학습 후 AI-Hub(v0)로 파인튜닝**하면 생성 품질·일반화가 나아지는지 검증한다.
앞선 단일시드 실험에서 보였던 "음의 전이"가 실제 효과인지, 저학습·시드 노이즈인지 **다중시드(42·1·2·3·4)**로 판정한다.

## 2. 설정
- 생성기: `set-transformer v2` (neural). 비교군: `baseline`(규칙기반).
- 조건: **noPretrain**(v0만 100ep) vs **preCubicasa**(global_cubicasa 50ep 사전학습 → v0 100ep 파인튜닝).
- 시드: 42·1·2·3·4. 각 학습 직후 eval + 일반화 진단을 원장에 누적.
- 규제 루프(reg_loop): off/on 각각 평가(on = 법규 위반 자동보정 루프).
- 평가셋: AI-Hub benchmark **동결 test**(전 버전 공통). 사전학습셋 = CubiCasa 정상 **3,018**장(당시).
- 지표: `adj_L1`(인접행렬 L1 거리, **낮을수록** GT에 가까움) · integrity(위상무결성) · legal(법규통과율) · diversity · novelty.

## 3. 결과 — 전체 test (시드 집계, mean±std)

| 구성 | 시드수 | adj_L1 ↓ | integrity | legal(off/on) | diversity | novelty |
|---|---|---|---|---|---|---|
| baseline | 13 | **0.069±0.000** | 1.000 | 0.94 / 1.00 | 1.000 | 1.000 |
| set-transformer v2 · noPretrain | 6 | 0.074±0.007 | 1.000 | 0.94 / 1.00 | 0.987 | 0.976 |
| set-transformer v2 · **preCubicasa** | 5 | 0.074±0.007 | 1.000 | 0.94 / 1.00 | 0.989 | 0.979 |
| (참고) v1 · noPretrain | 1 | 0.141±0.004 | 1.000 | 0.94 / 1.00 | 0.997 | 0.999 |
| (참고) v1 · preCubicasa | 1 | 0.189 | 1.000 | 0.94 / 1.00 | 0.998 | 0.998 |

- **integrity 전 구성 1.000** — 위상 무결성은 모두 통과(규칙엔진이 보장).
- **legal**: 규제 루프 off 0.94 → **on 1.00** — 법규 위반 자동보정 루프가 채광 등 위반을 100% 교정.
- v1→v2 아키텍처 개선으로 neural adj_L1이 0.14→0.074로 크게 향상(baseline 0.069에 근접).

## 4. 결과 — 일반화 (seen / unseen program)

| 구성 | 시드수 | seen adj_L1 ↓ | unseen adj_L1 ↓ |
|---|---|---|---|
| baseline | 13 | **0.059±0.000** | 0.266±0.000 |
| set-transformer v2 · noPretrain (T0.85) | 6 | 0.112±0.025 | 0.260±0.038 |
| set-transformer v2 · **preCubicasa** | 5 | **0.101±0.012** | **0.247±0.020** |

- seen = 학습에서 본 방 구성(program), unseen = 처음 보는 구성.

## 5. 해석·결론
1. **전체 test에서 글로벌 사전학습은 중립** — preCubicasa와 noPretrain이 0.074±0.007로 **동일**. → 앞선 "음의 전이"는 **저학습·단일시드 노이즈**였음이 다중시드로 확인됨(neural≈baseline 수준).
2. **일반화에선 사전학습이 작지만 일관된 이득** — unseen 0.247 vs 0.260, seen 0.101 vs 0.112, **분산도 더 작음**(±0.020 vs ±0.038). 처음 보는 평면 구성에 더 강건하고 시드 안정성↑.
3. 단, 차이가 대략 1σ 내라 **강한 신호는 아님**. 사전학습 데이터가 CubiCasa **3,018장**으로 작은 게 한 원인일 수 있음.

## 6. 한계
- 사전학습셋이 CubiCasa 3,018장으로 소규모 → 사전학습 이득의 상한을 못 봤을 가능성.
- baseline(규칙기반)이 전체 adj_L1·seen에서 여전히 최저 — neural의 의의는 **다양성·생성능력**과 unseen 강건성에 있음.
- 평가는 AI-Hub 동결 test 한정. 글로벌셋 자체에 대한 평가는 별도.

## 7. 후속 (재학습 시 업데이트)
- 🔜 **RPLAN 80,371장 사전학습 재검증**: 2026-06-04 RPLAN 전량 변환 완료(CubiCasa의 ≈27배). `train_gen --pretrain`에 RPLAN 소스를 물려 동일 다중시드 매트릭스 재실행 → 사전학습 데이터 규모가 일반화 이득을 키우는지 확인. **결과 나오면 본 문서 §3·§4 표에 행 추가.**
- 글로벌(RPLAN+CubiCasa) 합산 사전학습도 후보.
