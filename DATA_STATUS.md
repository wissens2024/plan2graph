# Plan2Graph 데이터 현황 (도면 데이터 위주)

> 2026-06-03 실측 기준. 모든 수치는 서버(115)에서 코드로 측정·검증됨.
> 검수 도구: https://plan2graph.aines.kr → "🔍 배제 도면 검수" (도면마다 시각 증거).

## 1. 데이터 출처
- **AI-Hub 건축 도면 데이터** (dataSetSn=71465). COCO 형식, UTF-8 라벨.
- 라벨 4종: **SPA**(공간=방 폴리곤) · **STR**(구조=출입문/창호/벽체) · OBJ(객체=변기·싱크대 등) · OCR(문자).
- 도면종류 4종: **FP**(평면도) · CS(단면도) · EP(입면도) · SD(구조도).
- 위상 그래프(방-문-방)는 **FP의 SPA+STR**이 둘 다 있어야 만들 수 있다.

## 2. 데이터 퍼널 (엔트리 → 고유 도면 → 그래프화) — 실측·확정

```
48,033 PNG 엔트리 (~41,556 FP 엔트리)      ← '엔트리'(중복 포함), 고유 도면 수 아님
   │  같은 도면이 SPA/STR/OBJ/OCR zip에 byte-identical 복제 → ~1.8배 부풀림
   ▼  내용지문(CRC32+크기)으로 dedup
고유 평면도(FP) ≈ 22,800  (Training 19,019 + Validation 3,778)
   ├─ Training 19,019
   │    ├─ 13,045 : SPA·STR 라벨 보유
   │    │     ├─ 3,836  dual(SPA+STR) ──────────► v0 그래프화 대상
   │    │     └─ 9,209  single(한쪽만) ──────────► V2V 복구 대상(부분배제)
   │    └─ 5,974 : OBJ/OCR만(방·구조 없음) ───────► 완전배제
   └─ Validation 3,778 (대부분 single/OBJ-OCR)
   +  비-FP 3,872(Train) : 단면도/입면도/구조도 ──► 완전배제(위상 자체 없음)
```

> **검증**: Training 13,045(SPA/STR) + 5,974(OBJ/OCR만) = **19,019** — 사업계획 funnel과 정확히 일치.

## 3. 고유 도면 dedup — 방법·정확성 (증명됨)
- **9자리 ID는 라벨종류 간 공유 키가 아님** (같은 도면도 SPA/STR에서 번호 다름) → 번호로 dedup 불가.
- 대신 **PNG의 CRC32+파일크기 지문**으로 묶음(= 파일 중복과 동일 알고리즘). zip 중앙디렉터리로 압축해제 없이 읽음.
- **전수 검증** (`scripts/verify_dedup.py`): dual 3,836개의 SPA-PNG vs STR-PNG SHA256 → **일치 3,836 / CRC 오충돌 0**. → dedup 정확.

## 4. 배제 분류 — 사유 + 수 (검수 GUI에 도면 증거)

| 구분 | 카테고리 | Training 수 | 사유 | 복구 |
|---|---|---|---|---|
| 🟢 채택 | dual(SPA+STR) | 3,836 | 방+구조 다 있음 → 그래프화 | (v0) |
| 🟡 부분배제 | 방만(spa_only) | 4,919 | 문·벽 라벨 없음 → 엣지 불가 | **V2V로 STR 예측** |
| 🟡 부분배제 | 구조만(str_only) | 4,290 | 방 라벨 없음 → 노드 불가 | **V2V로 SPA 예측** |
| 🔴 완전배제 | OBJ/OCR만 | 5,974 | 객체·문자만, 방·구조 없음 | (둘다 예측 필요, 우선순위 낮음) |
| 🔴 완전배제 | 비-FP | 3,872 | 단면도/입면도/구조도 = 평면도 아님 | 불가(위상 없음) |
| 🔴 완전배제 | 중복(복사본) | 89 | 같은 PNG가 한 라벨서 여러 키 | 1장만 채택 |

## 5. v0 데이터셋 (현재 산출물 — 라벨 기반)
- **세대 그래프 5,648개** (시트 2,636장). dual 3,836 도면에서 다세대 분해.
- 정상 필터: **현관==1(단일세대)**, 다세대 병합 제외(multi_household 305). 분할 train/val/test = **4,531 / 599 / 518**(누수 방지, test 동결).
- scale(㎡) 확보 3,656. 무결성 100%, 채광 법규 통과 95%.

## 6. V2V 복구 (벡터 추출로 single 라벨 채움) — 진행 상황
- 목표: **single 11,209장**(SPA-only/STR-only)의 빠진 라벨을 YOLO-seg로 예측 → 그래프화 도면 3,899 → **~15,000+**.
- 현재(스모크): SPA(방) 예측기 **mask mAP50 0.90**(우수), STR(문·벽) 0.47(5ep, 50ep+ 필요). 30장 추론→빌드 검증(97.3% 채택).
- 남음: STR 본학습 → 전체 추론 11,209 → v2 빌드 → v0 vs v2(양 vs 질) 평가.

## 7. 검수 증거 도구 (정부과제 — 수치마다 근거 도면)
- `https://plan2graph.aines.kr` → "🔍 배제 도면 검수": 배제 6종을 **실제 PNG + 라벨 오버레이**(방=초록·문=빨강·창=주황·벽=파랑)로 확인. 거주형태(APT/DEH/ROW) 필터.
- "⚠ 격리": 품질게이트 격리 도면 = 도면+그래프+사유로 검수·교정.
- 시작: `bash scripts/start_dashboard.sh` (115 서버).
